import React from "react";
import './index.css'

const SearchBox = function()
{
    return (
        <input className="centerElement" type= 'search' placeholder='Serch Robots'/>
    );
}
export default SearchBox;